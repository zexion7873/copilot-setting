config:
  # Allow any heading style
  MD003: false
  # Allow long lines (skill descriptions are naturally long)
  MD013: false
  # Allow duplicate headings in different files
  MD024: false
  # Allow HTML in markdown (we use <div>, <details>, etc.)
  MD033: false
  # Allow bare URLs
  MD034: false
  # Allow emphasis as heading
  MD036: false
  # Allow first line to be HTML (centered div with badges)
  MD041: false
  # Allow inline HTML images
  MD045: false
